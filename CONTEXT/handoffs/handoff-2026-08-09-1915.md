# Handoff — jump fix (laptop + phone)

**Date:** 2026-08-09  
**Branch:** `main`  
**Remote:** `origin` → `https://github.com/harshguptaindex1210-dot/kill-enemy.git`  
**Prior HEAD:** `fc3c2be` — README cache-bust `294a945`

## Current objective

Jump must work reliably on **laptop (Space)** and **phone (⬆ touch button)** during `dropping` and `playing`. User reported jump still broken after prior latch/z-index fixes (`007ec7d`, `7089fdb`).

## Live URLs

| Use | URL |
|-----|-----|
| **Cache-bust (after push)** | https://harshguptaindex1210-dot.github.io/kill-enemy/?v=<commit-sha> |
| **Direct** | https://harshguptaindex1210-dot.github.io/kill-enemy/ |

Hard refresh on laptop: `Ctrl + Shift + R`.

## What changed this session

| Area | Files | Change |
|------|-------|--------|
| **Ground contact** | `src/player.ts` | `<=` landing snap; coyote-time re-ground; `resetGroundContact()` on respawn |
| **Desktop Space** | `src/input.ts` | 600ms jump latch; pointer-lock loss clears movement keys only (not latch) |
| **Phone jump** | `src/input.ts` | Touch overlay `z-index:10010` (above HUD); `pointerdown`/`click` on ⬆ |
| **Respawn** | `src/gameplay.ts` | `respawnUnit` calls `resetGroundContact(GROUND_Y)` |
| **Online demo pose** | `src/net/onlineGame.ts` | Local mode reads live sim position; velocity extrapolation between 20Hz ticks |
| **Tests** | `tests/player.test.ts`, `tests/touchJump.test.ts`, `tests/onlineGame.test.ts`, `tests/gameplay.test.ts` | Ground/respawn jump coverage; latch/z-index assertions; sim pose test |

## Verification (already run)

```text
npm test  → 415 passed (56 files)
npm run build → raw 673326 / 675000 OK, gzip 181492 / 200000 OK
```

## How to verify manually

1. **Laptop — Play Local:** wait for **JUMP!** banner → click canvas → **Space** → character rises.
2. **Phone — landscape:** tap **⬆** on right touch pad during match.
3. **Online demo:** Play Online (Nakama fallback) — same jump paths via `OnlineMatchGame`.

## What remains

| Priority | Item |
|----------|------|
| P0 | User QA on real phone + laptop after deploy |
| P1 | Photo textures / city building meshes |
| P2 | Nakama online host (not just demo fallback) |
| P3 | Workspace files showing `git status` modified with **no content diff** (line-ending noise on Windows) — safe to `git checkout` or normalize `.gitattributes` |

## Known blockers / risks

- **Play Online (real Nakama):** server Lua jump path exists (`nakama/modules/match_handler.lua`); not redeployed this session.
- **Touch laptops:** touch UI mounts only on `isPhoneDevice()`; Space is the jump path.
- **Gauntlet critic:** same-context review only; no blind browser screenshot compare run.

## Important paths

| Path | Role |
|------|------|
| `src/input.ts` | Space latch, touch ⬆, overlay z-index |
| `src/player.ts` | Jump velocity, ground contact |
| `src/gameplay.ts` | `dropping` phase human input, respawn |
| `src/net/onlineGame.ts` | Online render loop, pose resolution |
| `src/game.ts` | Local match loop (`MatchGame`) |
| `tests/touchJump.test.ts` | Static wiring checks for mobile jump |

## Suggested skills for next agent

- `gauntlet-loop` — if jump still fails after deploy, run critic loop with browser screenshots
- `qa` — interactive device QA with user
- `push-handoff` — after next verified lap

## Invariants (do not regress)

- Phone landscape gate; touch joystick; HEAL + RESPAWN on touch HUD
- Minimap top-left; strafe right = +X; 5 med-kits at start
- Boot overlay only before `__killEnemyBooted`
- Three maps in settings v3; low quality skips post-processing
