# Handoff — selectable maps + GTA-style Los Santos city

**Date:** 2026-08-08 23:08 IST  
**Branch:** `main` @ `49025e5`  
**Remote:** `origin` → `https://github.com/harshguptaindex1210-dot/kill-enemy.git`  
**Prior handoff:** `CONTEXT/handoffs/2026-08-08-session-complete.md` @ `baa301b`

## Current objective

Ship **player-chosen maps** with a **GTA V open-world city** look (Los Santos), while keeping meadow and desert options. Map choice must persist in settings and apply on the next match start.

## Live URLs

| Use | URL |
|-----|-----|
| **Cache-bust (current)** | https://harshguptaindex1210-dot.github.io/kill-enemy/?v=49025e5 |
| **Direct** | https://harshguptaindex1210-dot.github.io/kill-enemy/ |

**Feature commit:** `49025e5` — selectable maps + Los Santos city visuals.

## What changed this session

### Feature: three selectable maps

| Map ID | Label | Visuals |
|--------|-------|---------|
| `meadow` | Meadow Valley | Existing BGMI hills/grass (default) |
| `city` | Los Santos | Asphalt, palms, parked cars, highway roads, urban POI scale |
| `desert` | Miramar Dunes | Sand ground, sparse trees, low grass |

### Where players pick maps

1. **Lobby** — map card grid (`lobby-map-panel`)
2. **Lobby settings** — `sel-map` dropdown
3. **In-match mobile** — ⚙ settings → `ms-map` selector

Map applies on **next match** (no mid-match scene swap). HUD shows map name in phase label.

### Files touched

| Area | Paths |
|------|-------|
| Map presets | `src/mapPresets.ts` (new) |
| Scene wiring | `src/scene.ts` — `createScene(canvas, quality, mapId)` |
| Graphics | `src/graphics.ts` — asphalt/sand textures, palms, parked cars |
| Urban POIs | `src/poiVisuals.ts` — `URBAN_PALETTE`, scaled city buildings |
| Settings v3 | `src/settings.ts` — `mapId`, storage key `robot_arena_settings_v3` |
| Lobby UI | `src/lobby.ts`, `src/lobby.css` |
| Mobile settings | `src/mobileSettingsPanel.ts` |
| Match HUD | `src/game.ts`, `src/net/onlineGame.ts` |
| Bundle gate | `scripts/bundle-size.js` — raw limit **662000 → 670000** |
| Tests | `tests/mapPresets.test.ts` (new), `tests/settings.test.ts`, `tests/graphics.test.ts` |

## Verification (green before push)

```text
npm test      → 404 passed (54 files), exit 0
npm run build → OK — raw 666174 / limit 670000, gzip 178920 / 200000
```

## What remains next

1. **Confirm Pages deploy** — after push, open `?v=<sha>` and pick Los Santos in lobby before starting a match.
2. **Graphics gauntlet R4** — robot material polish / muzzle FX (~3.8 KB bundle headroom left).
3. **Optional** — lobby test assertions for map card panel; mid-match map swap (large scope).
4. **Bundle relief** — trim strings or async-split before next major JS feature.

## Commands run

```bash
npm test          # 404 passed
npm run build     # bundle OK @ 666174 raw
git status        # feature + handoff staged on main
```

## Blockers, risks, assumptions

| Item | Detail |
|------|--------|
| Bundle headroom | ~3826 bytes raw before 670k cap — watch gate on next feature |
| GitHub Projects | `read:project` scope missing; `/work` uses handoff frontier |
| Pages online mode | Demo `LocalServer`, not live Nakama |
| Map sync online | Map is client-local setting; not synced across players in demo online |
| Founder name | Client-side only (`HARSH FOUNDERCEO_01`) |

## Invariants (do not regress)

- Phone landscape gate; touch joystick + look; HEAL + RESPAWN on touch HUD
- Jump 450ms latch; touch overlay z-index 10000
- Minimap **top-left**; strafe right = right; 5 med-kits at start
- Three.js only — no custom WebGL rewrite

## Suggested skills for next agent

| Skill | When |
|-------|------|
| `.cursor/skills/kill-enemy/SKILL.md` | Any Kill Enemy task |
| `gauntlet-loop` | Graphics R4 robot polish under bundle cap |
| `push-handoff` | After next verified slice |
| `qa` | Phone map picker + Los Santos flyover UAT |

## Context bundle index

- `CONTEXT/CONTEXT.md` — invariants INV-1..7
- `CONTEXT/CONTEXT-MAP.md` — file map (updated to this handoff)
- `CONTEXT/docs/DEPLOY.md` → root `DEPLOY.md`
- `.cursor/skills/kill-enemy/SKILL.md` — agent catalog
