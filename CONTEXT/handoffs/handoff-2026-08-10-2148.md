# Handoff — jump + lag fix (attempt 2)

**Date:** 2026-08-10  
**Branch:** `main`  
**Remote:** `https://github.com/harshguptaindex1210-dot/kill-enemy.git`

## Objective

Players reported **jump still broken** and **too much lag** on phone/laptop after the first 60fps demo-local pass (`100191c`). Root cause for jump was visual: demo-online camera never rose with the player.

## What changed this session

### Jump (real fix)

| File | Change |
|------|--------|
| `src/net/onlineGame.ts` | `updateCamera` now uses `localEyeHeight()` → `player.getEyeHeight()` instead of hardcoded `1.6` |
| `src/net/onlineGame.ts` | Removed broken `getSelfPose` extrapolation (`performance.now()` vs sim `time_ms`) |
| `src/player.ts` | Jump buffer extended (24 ticks) with air decay |

### Lag

| File | Change |
|------|--------|
| `src/net/onlineGame.ts` | Force **low** quality on mobile for online matches |
| `src/net/onlineGame.ts` | Throttle remote soldier anim updates every other frame on mobile |
| `src/net/client.ts` | Demo-local bot count **5** on mobile (was 9) |

### Tests

- `tests/onlineGame.test.ts` — camera must use `localEyeHeight` / `getEyeHeight`
- `tests/gameplay.test.ts` — stabilize melee integration test (facing + cadence)

## Verification

```text
npm test   → 420 passed
npm run build → OK (raw 674540 / 675000)
```

## Live link

After push: `https://harshguptaindex1210-dot.github.io/kill-enemy/?v=<HEAD_SHA>`

## Remaining / QA for user

1. **Hard refresh** or open with `?v=` cache-bust — stale `index.html` can load old JS.
2. Confirm on **Play Online** (demo): Space / ⬆ should visibly lift camera + soldier.
3. If lag persists on very old phones: lobby → **Low · Max FPS**.
4. **Play Local** was already using `getEyeHeight()` — issues were mainly demo-online path.

## Suggested skills for next agent

- `gauntlet-loop` — device QA loop with user screenshots
- `diagnosing-bugs` — if jump still fails on a specific device/browser
- `push-handoff` — after any follow-up fix

## Risks

- Bundle headroom ~460 bytes under limit — avoid large JS additions.
- Melee gameplay test is timing-sensitive; stabilized but not formally de-flaked.
