# Handoff — #45 Done (bench-render + INV-1)

**Date:** 2026-08-06  
**Ticket:** #45 → **Done**

## Hotspot note (measured)

Bench harness: Play Local → 300-frame rAF median in headless Chrome (SwiftShader).

| Preset | Median fps | Target |
|--------|------------|--------|
| low | ~120 | ≥30 |
| medium | ~120 | ≥60 |

Likely real-match cost centers (code review, not rewritten this ticket):

- Medium preset: 2048² directional shadow map + ~200 vegetation meshes with `castShadow`
- Per-frame `syncUnitRig` over all units + loot bob sine updates
- Held-weapon meshes are lightweight; local-only attach (INV-W2)

Default settings quality remains `medium` (`settings.ts`).

## Changes

- `scripts/bench-render.js` — preview server, Chrome path fallback, Play Local before measure
- `src/main.ts` — `?quality=` URL override for bench presets

## Gate

`npm test -- tests/held-weapons.test.ts && node scripts/bench-render.js low && node scripts/bench-render.js medium && npm run typecheck && npm run lint && npm run build` → exit 0

## Board

Queue empty (#44, #45, #46, #47, #48 all Done).
