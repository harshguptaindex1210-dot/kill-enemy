# Handoff — phone 404 + stale deploy cache fix

**Date:** 2026-08-09  
**Branch:** `main`  
**Remote:** `origin` → `https://github.com/harshguptaindex1210-dot/kill-enemy.git`  
**Prior:** `bd1bf19` — jump fix + README `98feab2`

## Current objective

Phone users opening the game saw GitHub **404 File not found** at the site root, or **Kill Enemy failed to load** when cached `index.html` pointed at a missing JS bundle hash.

## Root cause

| Symptom | Cause |
|---------|--------|
| 404 at `harshguptaindex1210-dot.github.io` | Game lives at `/kill-enemy/`; root had no `404.html` redirect on user Pages repo |
| Boot error overlay on phone | Stale cached `index.html` referenced old `index-*.js` (404) while deploy had new hash |

## What changed this session

| Area | Change |
|------|--------|
| **User site** | Pushed `404.html` to `harshguptaindex1210-dot.github.io` → redirects to `/kill-enemy/` |
| **Project 404** | `public/404.html` copied to dist on build |
| **Cache bust** | `VITE_CACHE_BUST` from `github.sha` in deploy.yml; `siteUrl.ts` appends `?v=` to share/play URLs |
| **Boot recovery** | `index.html` reloads once if main script 404s; boot error links to direct game URL |

## Live URLs (after push)

| Use | URL |
|-----|-----|
| **Phone / share** | https://harshguptaindex1210-dot.github.io/kill-enemy/?v=a9da5ee |
| **Root (redirects)** | https://harshguptaindex1210-dot.github.io/ |

## Verification

```text
npm test  → 416 passed
npm run build → OK
```

Manual: open root URL on phone → should land on lobby, not GitHub 404.

## What remains

- User QA on phone after deploy completes (~1 min)
- Jump fix from `98feab2` still needs device confirmation
- 12 workspace files with line-ending-only `git status` noise (unchanged content)

## Suggested skills

- `gauntlet-loop` — browser screenshot verify on phone viewport after deploy
- `qa` — user confirms link opens in WhatsApp / Chrome mobile
